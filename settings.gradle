include "core", "dependency-injection", "dependency-injection-android", "dependency-injection-desktop", "ecs", "ecs-integration", "tiled", "uats"
rootProject.name = 'mini2Dx'
rootProject.children.each { it.name = rootProject.name + "-" + it.name }
