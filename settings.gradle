include "core", "libgdx", "libgdx-desktop", "libgdx-android", "tiled", "ui", "artemis-odb", "all", "monogame", "uats", "uats-libgdx-desktop", "uats-libgdx-android"

rootProject.name = 'mini2Dx'
rootProject.children.each { it.name = rootProject.name + "-" + it.name }
