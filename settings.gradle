import org.apache.tools.ant.taskdefs.condition.Os

include "core", "libgdx", "libgdx-desktop", "tiled", "ui", "all", "monogame"

rootProject.name = 'mini2Dx'
rootProject.children.each { it.name = rootProject.name + "-" + it.name }